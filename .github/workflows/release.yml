name: Release

on:
  release:
    types: [created]

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always
  PROTOC_VERSION: "25.1"

jobs:
  # --------------- Linux targets (cross-compiled) ---------------
  build-linux:
    strategy:
      matrix:
        target:
          - x86_64-unknown-linux-gnu
          - aarch64-unknown-linux-gnu
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable

      - uses: arduino/setup-protoc@v3
        with:
          version: ${{ env.PROTOC_VERSION }}

      - name: Install cross
        run: cargo install cross --git https://github.com/cross-rs/cross

      - name: Build
        run: cross build --release --target ${{ matrix.target }}

      - name: Package
        run: |
          TAG="${{ github.event.release.tag_name }}"
          for bin in oxmon-agent oxmon-server; do
            tar czf "${bin}-${TAG}-${{ matrix.target }}.tar.gz" \
              -C "target/${{ matrix.target }}/release" "${bin}"
          done

      - uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.target }}
          path: "*.tar.gz"

  # --------------- macOS targets (native compilation) ---------------
  build-macos:
    strategy:
      matrix:
        include:
          - target: x86_64-apple-darwin
            os: macos-latest
          - target: aarch64-apple-darwin
            os: macos-latest
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@v4

      - uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - uses: arduino/setup-protoc@v3
        with:
          version: ${{ env.PROTOC_VERSION }}

      - uses: Swatinem/rust-cache@v2
        with:
          key: ${{ matrix.target }}

      - name: Build agent
        run: cargo build --release --target ${{ matrix.target }} --bin oxmon-agent

      - name: Package
        run: |
          TAG="${{ github.event.release.tag_name }}"
          tar czf "oxmon-agent-${TAG}-${{ matrix.target }}.tar.gz" \
            -C "target/${{ matrix.target }}/release" oxmon-agent

      - uses: actions/upload-artifact@v4
        with:
          name: binaries-${{ matrix.target }}
          path: "*.tar.gz"

  # --------------- Create GitHub Release ---------------
  release:
    needs: [build-linux, build-macos]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/download-artifact@v4
        with:
          pattern: binaries-*
          merge-multiple: true

      - name: Create checksums
        run: sha256sum *.tar.gz > SHA256SUMS

      - uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: |
            *.tar.gz
            SHA256SUMS
